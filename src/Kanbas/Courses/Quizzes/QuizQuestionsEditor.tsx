export default function QuizQuestionsEditor({ quiz, setQuiz, saveQuiz }: { quiz: any; setQuiz: (quiz: any) => void; saveQuiz: (quiz: any) => void }) {
    return (
        <div>
            Quiz Questions Editor
        </div>
    );
}